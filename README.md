# fn-packweb-docker

fnos-pack/
├── fnos-pack-web.py          # WebUI 代码
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── data/
│   ├── fnpack/               # 存放 Linux 版 fnpack 二进制
│   ├── output/               # 生成的项目和 .fpk 输出
│   └── cache/                # 缓存（可选）
└── 启动说明.txt
